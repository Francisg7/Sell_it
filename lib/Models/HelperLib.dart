class HelperLib {
  getIdFromName(String userName) {
    //This method will be used to get the user ID/Token from its Name
  }

  checkUserSignInStatus(userID) {
    //This method will be used to check if the user is still logged in
  }

  retrieveChats(userID) {
    //This method wil retrieve the list of cahts of a user from firebase
  }

  retrieveMessages(chatID) {
    //This funtion will return a stream of all the new messages from a particular chat
  }

  checkUserOnlineStatus(userID) {
    //This method will check if a user on the chat list is online.
  }
}
